{
  "name": "@tts-platform/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "vite build",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/static": "^8.3.0",
    "@tts-platform/core": "workspace:*",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vite": "^7.2.7"
  }
}
